# Скопируйте в файл .env при необходимости (не коммитьте .env).
#
# По умолчанию Vite слушает 5174 (как ссылки из писем). Другой порт:
# VITE_DEV_PORT=5173
# и тот же хост в server/.env → CLIENT_URL.
#
# Продакшен / API на другом домене:
# VITE_API_URL=https://api.ваш-домен.ru
